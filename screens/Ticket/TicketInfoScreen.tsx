import { View, Text } from "react-native";
import React from "react";

const TicketInfoScreen = () => {
  return (
    <View>
      <Text>TicketInfoScreen</Text>
    </View>
  );
};

export default TicketInfoScreen;
